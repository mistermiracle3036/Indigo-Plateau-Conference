# test_build.sh staging dir and the zips it produces
.build/
*.zip

# never commit ROM-derived content -- these are engine build products,
# not mod assets, and modkit pack refuses them
assets/generated/

.DS_Store
Thumbs.db

# test_build.sh also emits a flat build/ tree next to .build/
build/
